definitions:
  billing:
    type: array
    items:
      type: object
      properties:
        productid:
          type : string
          # required: true
        quantity:
          type: number
          # required: true