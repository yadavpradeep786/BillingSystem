definitions:
  user:
    properties:
      email:
        type: "string"
        format: email
      password:
        type: "string"
        format: password
      firstName:
        type: "string"
      lastName:
        type: "string"
